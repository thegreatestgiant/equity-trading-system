---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tailscale-ui-entry
  namespace: frontend # Streamlit lives here
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - ui
  rules:
    - host: ui
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: traefik-proxy # Point to our double-proxy bridge
                port:
                  number: 80
